const daughters = ["Valeska", "Cibalena"]
const sons = ["Auror", "Tilsdo"]

const all = daughters.concat(sons, "Cicrano")
console.log(all, daughters, sons)

console.log([].concat([1, 2], [3, 4], 5, [[6, 7]]))